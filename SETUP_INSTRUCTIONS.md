# 📚 คู่มือการติดตั้งระบบ Inventory Management

## 🎯 ภาพรวม
ระบบนี้เชื่อมต่อระหว่าง Web Application กับ Google Sheets ผ่าน Google Apps Script

---

## 📊 โครงสร้าง Google Sheets

ระบบต้องการ 3 Sheets หลัก:

### 1. **Inventory** (สินค้าคงคลัง)
| Column | ชื่อ | รายละเอียด |
|--------|------|-----------|
| A | Barcode | รหัสบาร์โค้ดสินค้า |
| B | Name | ชื่อสินค้า |
| C | Category | หมวดหมู่ |
| D | Stock | จำนวนคงเหลือ |
| E | Unit | หน่วยนับ (ชิ้น, ขวด, กล่อง) |
| F | Price | ราคาต่อหน่วย |
| G | Min_Stock | จำนวนขั้นต่ำ (แจ้งเตือน) |
| H | Image_URL | URL รูปภาพสินค้า |
| I | Last_Updated | วันที่อัปเดตล่าสุด |

### 2. **Transaction_History** (ประวัติการเคลื่อนไหว)
| Column | ชื่อ | รายละเอียด |
|--------|------|-----------|
| A | Timestamp | วันเวลาที่ทำรายการ |
| B | Barcode | รหัสบาร์โค้ด |
| C | Product_Name | ชื่อสินค้า |
| D | Type | ประเภท (IN/OUT) |
| E | Amount | จำนวน |
| F | Previous_Stock | สต็อกก่อนหน้า |
| G | New_Stock | สต็อกใหม่ |
| H | User_ID | ผู้ทำรายการ |
| I | Image_URL | URL รูปภาพประกอบ |

### 3. **Reports** (รายงานสรุป)
| Column | ชื่อ | รายละเอียด |
|--------|------|-----------|
| A | Report_Date | วันที่สร้างรายงาน |
| B | Total_Products | จำนวนสินค้าทั้งหมด |
| C | Total_Value | มูลค่ารวม |
| D | Low_Stock_Count | จำนวนสินค้าที่ใกล้หมด |
| E | Notes | หมายเหตุ |

---

## 🔧 ขั้นตอนการติดตั้ง Google Apps Script

### ขั้นตอนที่ 1: เปิด Apps Script Editor

1. เปิด Google Sheets: https://docs.google.com/spreadsheets/d/1c3U81eazLDTMQTdDScObASikKgYJf_qmKabn4Lyf1Og/edit
2. คลิกเมนู **Extensions** (ส่วนขยาย)
3. เลือก **Apps Script**

### ขั้นตอนที่ 2: วาง Code

1. ลบโค้ดเดิมทั้งหมดในไฟล์ `Code.gs`
2. เปิดไฟล์ `google-apps-script/Code.gs` ในโปรเจกต์นี้
3. คัดลอกโค้ดทั้งหมด
4. วางลงใน Apps Script Editor
5. คลิก **💾 Save** (Ctrl+S)

### ขั้นตอนที่ 3: รัน Setup Function (ครั้งแรกเท่านั้น)

1. ในตัวแก้ไข Apps Script
2. เลือกฟังก์ชัน **`setupSheets`** จาก dropdown ด้านบน
3. คลิกปุ่ม **Run** (▶️)
4. **อนุญาตสิทธิ์:**
   - คลิก "Review permissions"
   - เลือกบัญชี Google ของคุณ
   - คลิก "Advanced" → "Go to [Project Name] (unsafe)"
   - คลิก "Allow"
5. รอให้ฟังก์ชันทำงานเสร็จ (ดูที่ Execution log)

### ขั้นตอนที่ 4: Deploy Web App

1. คลิกปุ่ม **Deploy** (ด้านบนขวา)
2. เลือก **New deployment**
3. คลิกไอคอน **⚙️ (เฟือง)** → เลือก **Web app**
4. ตั้งค่า:
   - **Description:** "Inventory Management API"
   - **Execute as:** **Me** (ตัวคุณเอง)
   - **Who has access:** **Anyone** (ทุกคน)
5. คลิก **Deploy**
6. **คัดลอก Web app URL** ที่ได้ (จะมีหน้าตาประมาณนี้):
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```

### ขั้นตอนที่ 5: อัปเดต Frontend Code

1. เปิดไฟล์ `src/services/api.js`
2. แทนที่ค่า `GAS_API_URL` ด้วย URL ที่คัดลอกมาจากขั้นตอนที่ 4:
   ```javascript
   const GAS_API_URL = "YOUR_WEB_APP_URL_HERE";
   ```
3. บันทึกไฟล์

---

## 🧪 ทดสอบการเชื่อมต่อ

### ทดสอบผ่าน Browser

เปิด URL นี้ในเบราว์เซอร์:
```
YOUR_WEB_APP_URL?action=getInventory
```

ควรได้ผลลัพธ์เป็น JSON:
```json
{
  "success": true,
  "data": [
    {
      "barcode": "8850123456789",
      "name": "สินค้าตัวอย่าง 1",
      "category": "อาหาร",
      "stock": 100,
      ...
    }
  ]
}
```

### ทดสอบผ่าน Web App

1. เปิด Web App: http://localhost:5173/
2. ไปที่หน้า Inventory
3. ควรเห็นข้อมูลสินค้าจาก Google Sheets

---

## 📝 API Endpoints

### GET Requests

| Action | URL | คำอธิบาย |
|--------|-----|---------|
| `getInventory` | `?action=getInventory` | ดึงข้อมูลสินค้าทั้งหมด |
| `getHistory` | `?action=getHistory` | ดึงประวัติการเคลื่อนไหว |
| `getReports` | `?action=getReports` | ดึงรายงานสรุป |
| `searchProduct` | `?action=searchProduct&query=xxx` | ค้นหาสินค้า |
| `getProduct` | `?action=getProduct&barcode=xxx` | ดึงข้อมูลสินค้าตามบาร์โค้ด |

### POST Requests

| Action | Parameters | คำอธิบาย |
|--------|-----------|---------|
| `updateStock` | barcode, amount, type, userId, imageUrl | อัปเดตสต็อก (IN/OUT) |
| `addProduct` | productData (JSON) | เพิ่มสินค้าใหม่ |
| `uploadImage` | imageData, fileName | อัปโหลดรูปภาพ |

---

## 🔄 การอัปเดต Apps Script

หากต้องการแก้ไขโค้ด:

1. แก้ไขไฟล์ `google-apps-script/Code.gs`
2. คัดลอกโค้ดใหม่
3. วางใน Apps Script Editor
4. บันทึก (Ctrl+S)
5. **Deploy ใหม่:**
   - คลิก **Deploy** → **Manage deployments**
   - คลิกไอคอน **✏️ (ดินสอ)** ข้างเวอร์ชันปัจจุบัน
   - เลือก **New version**
   - คลิก **Deploy**

---

## ⚠️ Troubleshooting

### ปัญหา: "Authorization required"
**วิธีแก้:** รันฟังก์ชัน `setupSheets` อีกครั้งและอนุญาตสิทธิ์

### ปัญหา: "Script function not found"
**วิธีแก้:** ตรวจสอบว่าคัดลอกโค้ดครบถ้วนและบันทึกแล้ว

### ปัญหา: Frontend ไม่แสดงข้อมูล
**วิธีแก้:** 
1. ตรวจสอบ `GAS_API_URL` ใน `src/services/api.js`
2. เปิด Console (F12) ดู error
3. ตรวจสอบว่า Deploy เป็น "Anyone" แล้ว

### ปัญหา: CORS Error
**วิธีแก้:** Google Apps Script รองรับ CORS อัตโนมัติ แต่ต้องแน่ใจว่า:
- Deploy เป็น Web app
- Who has access = "Anyone"

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย สามารถ:
1. ตรวจสอบ Execution log ใน Apps Script
2. เปิด Browser Console (F12) ดู error
3. ตรวจสอบ Network tab ว่า request ส่งถูกต้องหรือไม่

---

## ✅ Checklist

- [ ] เปิด Apps Script Editor
- [ ] วาง Code จากไฟล์ `Code.gs`
- [ ] รันฟังก์ชัน `setupSheets`
- [ ] อนุญาตสิทธิ์ทั้งหมด
- [ ] Deploy เป็น Web app
- [ ] คัดลอก Web app URL
- [ ] อัปเดต `GAS_API_URL` ใน `api.js`
- [ ] ทดสอบการเชื่อมต่อ
- [ ] ตรวจสอบว่า 3 Sheets ถูกสร้างแล้ว

---

**สำเร็จ! 🎉** ระบบพร้อมใช้งานแล้ว
