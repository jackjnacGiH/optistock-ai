# 📱 คู่มือการ Deploy เพื่อใช้งานบนมือถือ

## 🌟 วิธีที่ 1: Deploy ด้วย Vercel (แนะนำที่สุด - ฟรี!)

Vercel เป็นแพลตฟอร์มที่ดีที่สุดสำหรับ Deploy Vite/React apps ฟรีและรวดเร็ว

### ขั้นตอนการ Deploy:

#### 1. สร้างบัญชี Vercel
1. ไปที่ https://vercel.com
2. คลิก **Sign Up**
3. เลือก **Continue with GitHub** (แนะนำ)

#### 2. Push โค้ดขึ้น GitHub

```bash
# เปิด Terminal ในโฟลเดอร์โปรเจกต์
cd "d:\Project Vibe codeAI\BarcodeScan\inventory-client"

# สร้าง Git repository (ถ้ายังไม่มี)
git init

# เพิ่มไฟล์ทั้งหมด
git add .

# Commit
git commit -m "Initial commit - Inventory Management App"

# สร้าง repository ใหม่บน GitHub
# ไปที่ https://github.com/new
# ตั้งชื่อ repository เช่น "inventory-app"
# อย่าเลือก "Initialize with README"

# เชื่อมต่อกับ GitHub (แทนที่ YOUR_USERNAME ด้วยชื่อ GitHub ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/inventory-app.git
git branch -M main
git push -u origin main
```

#### 3. Deploy บน Vercel

1. ไปที่ https://vercel.com/dashboard
2. คลิก **Add New** → **Project**
3. เลือก repository **inventory-app** ที่เพิ่งสร้าง
4. Vercel จะตรวจจับ Vite โดยอัตโนมัติ
5. คลิก **Deploy**
6. รอ 1-2 นาที
7. คุณจะได้ URL เช่น `https://inventory-app-xxx.vercel.app`

#### 4. เปิดใช้งานบนมือถือ

1. เปิดเบราว์เซอร์บนมือถือ (Chrome, Safari)
2. เข้า URL ที่ได้จาก Vercel
3. **เพิ่มไปยังหน้าจอหลัก** (Add to Home Screen):
   - **iOS**: กด Share → Add to Home Screen
   - **Android**: กด Menu (⋮) → Add to Home Screen
4. ตอนนี้คุณมีแอปบนหน้าจอหลักแล้ว! 🎉

---

## 🏠 วิธีที่ 2: ใช้ในเครือข่ายเดียวกัน (LAN)

ใช้ได้เมื่อคอมพิวเตอร์และมือถืออยู่ใน WiFi เดียวกัน

### ขั้นตอน:

#### 1. หา IP Address ของคอมพิวเตอร์

```powershell
# เปิด PowerShell และรันคำสั่ง:
ipconfig

# มองหา "IPv4 Address" ภายใต้ WiFi adapter
# จะได้เลขประมาณ: 192.168.1.100
```

#### 2. แก้ไข Vite Config

แก้ไขไฟล์ `vite.config.js`:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',  // เพิ่มบรรทัดนี้
    port: 5173
  }
})
```

#### 3. รัน Dev Server

```bash
npm run dev
```

#### 4. เปิดบนมือถือ

1. เชื่อมต่อมือถือกับ WiFi เดียวกับคอมพิวเตอร์
2. เปิดเบราว์เซอร์บนมือถือ
3. ไปที่: `http://192.168.1.100:5173` (แทนที่ด้วย IP ของคุณ)
4. เพิ่มไปยังหน้าจอหลัก (Add to Home Screen)

**ข้อจำกัด:**
- ต้องเปิดคอมพิวเตอร์ทิ้งไว้
- ต้องอยู่ใน WiFi เดียวกัน
- ไม่สามารถใช้งานนอกบ้านได้

---

## ☁️ วิธีที่ 3: Deploy ด้วย Netlify (ทางเลือกอื่นของ Vercel)

### ขั้นตอน:

1. ไปที่ https://www.netlify.com
2. Sign up ด้วย GitHub
3. คลิก **Add new site** → **Import an existing project**
4. เลือก repository จาก GitHub
5. Build settings:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. คลิก **Deploy**
7. คุณจะได้ URL เช่น `https://inventory-app.netlify.app`

---

## 📦 วิธีที่ 4: Build เป็น PWA (Progressive Web App)

ทำให้เว็บแอปทำงานเหมือนแอปจริงๆ แม้ไม่มีอินเทอร์เน็ต

### ขั้นตอน:

#### 1. ติดตั้ง Vite PWA Plugin

```bash
npm install -D vite-plugin-pwa
```

#### 2. แก้ไข `vite.config.js`

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: 'Inventory Management',
        short_name: 'Inventory',
        description: 'Barcode Inventory Management System',
        theme_color: '#3b82f6',
        background_color: '#ffffff',
        display: 'standalone',
        icons: [
          {
            src: '/icon-192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: '/icon-512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ]
})
```

#### 3. สร้างไอคอน

สร้างไฟล์ `public/icon-192.png` และ `public/icon-512.png`

#### 4. Deploy

Deploy ด้วย Vercel หรือ Netlify ตามวิธีที่ 1 หรือ 3

---

## 🔒 วิธีที่ 5: ใช้ ngrok (สำหรับทดสอบชั่วคราว)

ใช้เมื่อต้องการให้คนอื่นทดสอบแอปชั่วคราว

### ขั้นตอน:

#### 1. ติดตั้ง ngrok

1. ไปที่ https://ngrok.com
2. Sign up และดาวน์โหลด ngrok
3. แตกไฟล์และวางใน folder ที่ต้องการ

#### 2. รัน Dev Server

```bash
npm run dev
```

#### 3. รัน ngrok

```bash
# เปิด Terminal ใหม่
ngrok http 5173
```

#### 4. ใช้งาน

- คุณจะได้ URL เช่น `https://abc123.ngrok.io`
- เปิด URL นี้บนมือถือได้เลย
- **ข้อจำกัด**: URL จะเปลี่ยนทุกครั้งที่รัน ngrok ใหม่ (ฟรีเวอร์ชัน)

---

## ✅ แนะนำสำหรับการใช้งานจริง

### สำหรับการใช้งานส่วนตัว:
1. **Deploy ด้วย Vercel** (วิธีที่ 1) ✨ **แนะนำที่สุด**
2. เพิ่ม PWA (วิธีที่ 4) เพื่อให้ใช้งานแบบ offline ได้

### สำหรับทดสอบเบื้องต้น:
1. **ใช้ LAN** (วิธีที่ 2) - ง่ายและรวดเร็ว
2. **ใช้ ngrok** (วิธีที่ 5) - ถ้าต้องการให้คนอื่นทดสอบ

---

## 📝 Checklist การ Deploy

- [ ] Push โค้ดขึ้น GitHub
- [ ] สร้างบัญชี Vercel/Netlify
- [ ] Deploy โปรเจกต์
- [ ] ทดสอบ URL ที่ได้
- [ ] เพิ่มไปยังหน้าจอหลักบนมือถือ
- [ ] ทดสอบการสแกนบาร์โค้ดบนมือถือ
- [ ] ทดสอบการอัปเดตสต็อก
- [ ] ตรวจสอบว่า Google Sheets อัปเดตถูกต้อง

---

## 🎯 ขั้นตอนที่แนะนำ (Quick Start)

```bash
# 1. Push ขึ้น GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/inventory-app.git
git push -u origin main

# 2. ไปที่ vercel.com และ deploy
# 3. เปิด URL บนมือถือ
# 4. Add to Home Screen
# 5. เสร็จสิ้น! 🎉
```

---

## ❓ คำถามที่พบบ่อย

### Q: ใช้ได้ฟรีจริงหรือ?
A: ใช่! Vercel และ Netlify มี Free tier ที่เพียงพอสำหรับแอปส่วนตัว

### Q: ต้องมีโดเมนเป็นของตัวเองไหม?
A: ไม่ต้อง! Vercel/Netlify ให้โดเมนฟรี เช่น `your-app.vercel.app`

### Q: กล้องสแกนบาร์โค้ดทำงานบนมือถือไหม?
A: ทำงาน! แต่ต้อง deploy ด้วย HTTPS (Vercel/Netlify รองรับอัตโนมัติ)

### Q: ข้อมูลปลอดภัยไหม?
A: ปลอดภัย! ข้อมูลเก็บใน Google Sheets ของคุณเอง

### Q: อัปเดตโค้ดยังไง?
A: แค่ push ขึ้น GitHub แล้ว Vercel จะ deploy ใหม่อัตโนมัติ

---

## 🚀 พร้อมแล้ว!

เลือกวิธีที่เหมาะกับคุณและเริ่มใช้งานได้เลย! 

**แนะนำ**: เริ่มจากวิธีที่ 1 (Vercel) เพราะง่ายที่สุดและใช้งานได้ดีที่สุด 🌟
